<Window x:Class="TaskTools.Views.RoutineList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTools.Views"
        xmlns:localVM="clr-namespace:TaskTools.ViewModels"
        mc:Ignorable="d"
        Title="Routines" Height="300" Width="300">
    <Window.DataContext>
        <localVM:RoutineListViewModel/>
    </Window.DataContext>
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <Button DockPanel.Dock="Right" Background="Transparent" BorderThickness="0" Margin="0,0,10,0"
                    Command="{Binding CreateRoutine}"
                    Visibility="{Binding IsEnabled, Converter={StaticResource BooleanToVisibilityConverter},
                                 RelativeSource={RelativeSource Self}}">
                <Image Source="..\Images\file+.png" Height="20" Width="20"/>
            </Button>
        </DockPanel>
        <DataGrid ItemsSource="{Binding Routines}" AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Task"/>
                <DataGridTextColumn Header="Start on every"/>
                <DataGridTextColumn Header="Stage"/>
                <DataGridTextColumn Header="Category"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
